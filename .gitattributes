# The sync files are generated by `d6s sync pull`. Marking them as such
# keeps them out of diff noise and out of language statistics, where a
# hundred kilobytes of machine-written JSON would otherwise drown the
# code somebody came to read.
directus/** linguist-generated=true
pnpm-lock.yaml linguist-generated=true -diff

# Demo portraits are binary.
*.png binary
